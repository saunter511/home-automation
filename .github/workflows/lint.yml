name: Lint

on: [push, pull_request_target]

jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
        uses: actions/checkout@v2
        run: cp frontend/package.json . 
        run: yarn
      - name: Run ESLint
        uses: eslint . --ext .js,.jsx,.ts,.tsx
